import React, { Component } from 'react';
import { Link } from 'react-router';

var Intro = class App extends Component {
    render() {
        return (
            <div className="intro">
            </div>
        );
    }
};

export default Intro;
